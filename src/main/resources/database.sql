CREATE  TABLE Student(
                        Id SERIAL PRIMARY KEY,
                        fullname VARCHAR(45),
                        gmail  VARCHAR(45),
                        numberphone VARCHAR(20),
                        password VARCHAR(30)
)